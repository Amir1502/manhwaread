package com.manhwaread.core.designsystem

import androidx.compose.material3.Typography

// Дефолтная шкала Material3 на системном шрифте: читалка для вписывания
// перевода использует собственный TextMeasurer (:core:vision-model),
// поэтому кастомные шрифты UI здесь не нужны.
val ManhwareadTypography = Typography()
